# RoadGuard local environment template
# Copy this file to .env for local Docker Compose execution.
# NEVER commit real credentials or production secrets.

MSSQL_PORT=1433
# MSSQL_SA_PASSWORD is REQUIRED. Set a strong password (minimum 8 characters, uppercase, lowercase, digits, special characters) before running docker compose.
MSSQL_SA_PASSWORD=
